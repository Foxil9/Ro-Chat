# Database Configuration
DB_URL=mongodb://localhost:27017/rochat

# Server Configuration
SERVER_URL=
PORT=3000

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your-secret-key-here

# Environment
NODE_ENV=development

# Roblox OAuth2 Configuration (Client Side)
# Get these from https://create.roblox.com/credentials
ROBLOX_CLIENT_ID=your_client_id_here

# OAuth2 Redirect URI (must match the one registered in your Roblox OAuth2 app)
# Default: http://localhost:3333/callback
OAUTH_REDIRECT_URI=http://localhost:3333/callback
OAUTH_CALLBACK_PORT=3333

# Note: ROBLOX_CLIENT_SECRET is NOT needed on client side for security
# The client secret is only required on the server (see server deployment docs)
